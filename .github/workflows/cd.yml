name: CD

on:
    push: 
        branches: 
            - deploy-workflow

jobs:
    build-docker:
        name: checkout
        runs-on: [self-hosted,Linux,X64]
        container: node:lts
        steps:
            - name: Checkout 🚣
              uses: actions/checkout@v2
            - name: Build docker image ⚡️
              run: docker build -t sit-prod .
                